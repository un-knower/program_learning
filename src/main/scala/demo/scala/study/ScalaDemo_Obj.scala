package demo.scala.study

object ScalaDemo_Obj {
    def main(args: Array[String]): Unit = {
        val p = new People
        //p.gender //访问不到了，但是在伴生对象中可以被访问到
        
        
        
      
    }
}